## context for domain wording

-- User enters a topic and a pipeline is created for creating content
-- user creates many pipelines.
-- collection of pipelines is called reels.
-- each item in the collection (reels) is a pipeline
